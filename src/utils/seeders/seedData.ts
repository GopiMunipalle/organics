import { seedCategory } from "./categorySeeders";
import {
  seedNotificationCat,
  seedSellerNotificationType,
} from "./notificationCategorySeeder";

export default async function seedData() {
  // seedCategory();
  //   seedNotificationCat();
  // seedSellerNotificationType();
}

// when user signed up
// add notification settings
